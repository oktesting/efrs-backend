//check whether the authenticated client is authorized (isAdmin: true)

describe('getRole middleware', () => {
  describe('isAdmin function', () => {});
  describe('isSupervisor function', () => {});
  describe('isUser function', () => {});
});
